import { PagePlaceholder } from '../components/PagePlaceholder'

/**
 * SeasonPass hosts /seasonpass — per-season monitored toggling across the
 * whole library. Pending UI.
 */
export function SeasonPass() {
  return (
    <PagePlaceholder
      title="Season Pass"
      description="Bulk toggle per-season monitored flags across every series. Backend per-season PUT already exists."
    />
  )
}

// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: indexers.sql

package postgres

import (
	"context"
)

const createIndexer = `-- name: CreateIndexer :one
INSERT INTO indexers (name, implementation, settings, enable_rss, enable_automatic_search, enable_interactive_search, priority)
VALUES ($1, $2, $3, $4, $5, $6, $7)
RETURNING id, name, implementation, settings, enable_rss, enable_automatic_search, enable_interactive_search, priority, added
`

type CreateIndexerParams struct {
	Name                    string
	Implementation          string
	Settings                []byte
	EnableRss               bool
	EnableAutomaticSearch   bool
	EnableInteractiveSearch bool
	Priority                int32
}

func (q *Queries) CreateIndexer(ctx context.Context, arg CreateIndexerParams) (Indexer, error) {
	row := q.db.QueryRow(ctx, createIndexer,
		arg.Name,
		arg.Implementation,
		arg.Settings,
		arg.EnableRss,
		arg.EnableAutomaticSearch,
		arg.EnableInteractiveSearch,
		arg.Priority,
	)
	var i Indexer
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Implementation,
		&i.Settings,
		&i.EnableRss,
		&i.EnableAutomaticSearch,
		&i.EnableInteractiveSearch,
		&i.Priority,
		&i.Added,
	)
	return i, err
}

const deleteIndexer = `-- name: DeleteIndexer :exec
DELETE FROM indexers WHERE id = $1
`

func (q *Queries) DeleteIndexer(ctx context.Context, id int32) error {
	_, err := q.db.Exec(ctx, deleteIndexer, id)
	return err
}

const getIndexerByID = `-- name: GetIndexerByID :one
SELECT id, name, implementation, settings, enable_rss, enable_automatic_search, enable_interactive_search, priority, added
FROM indexers
WHERE id = $1
`

func (q *Queries) GetIndexerByID(ctx context.Context, id int32) (Indexer, error) {
	row := q.db.QueryRow(ctx, getIndexerByID, id)
	var i Indexer
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Implementation,
		&i.Settings,
		&i.EnableRss,
		&i.EnableAutomaticSearch,
		&i.EnableInteractiveSearch,
		&i.Priority,
		&i.Added,
	)
	return i, err
}

const listIndexers = `-- name: ListIndexers :many
SELECT id, name, implementation, settings, enable_rss, enable_automatic_search, enable_interactive_search, priority, added
FROM indexers
ORDER BY name
`

func (q *Queries) ListIndexers(ctx context.Context) ([]Indexer, error) {
	rows, err := q.db.Query(ctx, listIndexers)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Indexer{}
	for rows.Next() {
		var i Indexer
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Implementation,
			&i.Settings,
			&i.EnableRss,
			&i.EnableAutomaticSearch,
			&i.EnableInteractiveSearch,
			&i.Priority,
			&i.Added,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateIndexer = `-- name: UpdateIndexer :exec
UPDATE indexers
SET name                      = $2,
    implementation            = $3,
    settings                  = $4,
    enable_rss                = $5,
    enable_automatic_search   = $6,
    enable_interactive_search = $7,
    priority                  = $8
WHERE id = $1
`

type UpdateIndexerParams struct {
	ID                      int32
	Name                    string
	Implementation          string
	Settings                []byte
	EnableRss               bool
	EnableAutomaticSearch   bool
	EnableInteractiveSearch bool
	Priority                int32
}

func (q *Queries) UpdateIndexer(ctx context.Context, arg UpdateIndexerParams) error {
	_, err := q.db.Exec(ctx, updateIndexer,
		arg.ID,
		arg.Name,
		arg.Implementation,
		arg.Settings,
		arg.EnableRss,
		arg.EnableAutomaticSearch,
		arg.EnableInteractiveSearch,
		arg.Priority,
	)
	return err
}
